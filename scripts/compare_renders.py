"""
compare_renders.py — visual regression helper (stub).

Planned: render samples/test.html with each phase enabled, capture
PNGs, diff against baseline. Useful when stroke geometry tuning across
phase changes risks regressions.

Usage (planned):
    python scripts/compare_renders.py --baseline tests/baseline/
"""
import sys

def main():
    print("compare_renders.py: not yet implemented", file=sys.stderr)
    sys.exit(1)

if __name__ == "__main__":
    main()
