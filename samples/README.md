# Samples

Open `test.html` in any modern browser after `make sample`. Toggle
phase checkboxes (ss01, ss02, ...) in the top bar to enable any
combination. Sliders adjust font size and line height.

To add new test surfaces (specific dialects, technical content,
multilingual text), drop additional self-contained HTML files here.
